[workspace]
resolver = "3"
members = ["core", "compiler", "driver", "vm"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-only"
repository = "https://github.com/trust-lang/trust"

[workspace.dependencies]
trit-core = { path = "core" }
