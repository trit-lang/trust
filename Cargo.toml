[workspace]
resolver = "3"
members = ["core", "compiler", "vm"]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/trust-lang/trust"

[workspace.dependencies]
trit-core = { path = "core" }
